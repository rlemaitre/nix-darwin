[
    {
        "label": "run tests with bloop",
        "name": "run-bloop-test",
        "command": "~/.config/zed/run-bloop-test.sh ${ZED_WORKTREE_ROOT} ${ZED_SYMBOL}",
        "working_directory": "${workspace_root}",
        "environment": {
            "PATH": "${env.PATH}"
        },
        "keystroke": "ctrl-shift-r",
        "tags": ["scala-test"]
    },
    {
        "label": "run main with bloop",
        "name": "run-main",
        "command": "~/.config/zed/run-bloop-main.sh ${ZED_WORKTREE_ROOT} ${ZED_FILE} ${ZED_SYMBOL}",
        "working_directory": "${workspace_root}",
        "environment": {
            "PATH": "${env.PATH}"
        },
        "tags": ["scala-main"]
    }
]
